<%= form_tag({controller: "dashboard", action: "john"}, method: "post", :remote => true) do %>
  <% if @john %>
    <%= image_submit_tag("john.png", class: 'btn active light-active', style: 'width: 100%') %>
  <% else %>
    <%= image_submit_tag("john.png", class: 'btn', style: 'width: 100%') %>
  <% end %>
<% end %>